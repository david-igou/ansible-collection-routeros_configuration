---
- name: Converge — run an arbitrary command (set system note)
  hosts: molecule
  gather_facts: false
  vars:
    routeros_command:
      - path: "system note"
        cmd: "set note=cmd-role-marker"
  roles:
    - role: david_igou.routeros_configuration.command
