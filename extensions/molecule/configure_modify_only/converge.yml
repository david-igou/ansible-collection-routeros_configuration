---
- name: Converge — modify-only path via configure
  hosts: molecule
  gather_facts: false
  vars:
    routeros_config:
      /ip/service:
        data:
          - name: telnet
            disabled: true
  roles:
    - role: david_igou.routeros_configuration.configure
