---
# Provisioner wiring, test_sequence, and verifier are centralised in
# extensions/molecule/config.yml and merged in by molecule. This file declares
# only the scenario name.
scenario:
  name: interface_bridge_settings
