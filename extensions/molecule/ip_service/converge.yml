---
- name: Converge — disable the built-in telnet service
  hosts: molecule
  gather_facts: false
  vars:
    routeros_ip_service:
      - name: telnet
        disabled: true
  roles:
    - role: david_igou.routeros_configuration.ip_service
