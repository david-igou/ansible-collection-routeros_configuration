# interface_list

Declaratively manage `interface list` over the RouterOS API.

    routeros_interface_list:
      - {name: WAN}

Requires the shared `routeros_api_*` connection vars (see the `_reconcile` role).
