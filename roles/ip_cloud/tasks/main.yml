---
# tasks file for david_igou.routeros_configuration.ip_cloud
- name: "Reconcile ip cloud"
  ansible.builtin.include_role:
    name: _reconcile
  vars:
    rcfg_path: ip cloud
    rcfg_data: "{{ routeros_ip_cloud }}"
    rcfg_purge: false
    rcfg_order: false
