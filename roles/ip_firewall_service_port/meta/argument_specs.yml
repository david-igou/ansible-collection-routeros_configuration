---
argument_specs:
  main:
    short_description: >-
      Manage /ip/firewall/service-port declaratively (modify-only).
    options:
      routeros_ip_firewall_service_port:
        type: list
        elements: dict
        default: []
        description: >-
          Built-in service-port helpers to modify (name, disabled, ports).
