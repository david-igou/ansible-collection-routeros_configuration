---
argument_specs:
  main:
    short_description: Manage /ip/settings declaratively (singleton).
    options:
      routeros_ip_settings:
        type: list
        elements: dict
        default: []
        description: >-
          Single-element list of global IP settings (ip-forward, ...).
