---
# tasks file for david_igou.routeros_configuration.ip_vrf
- name: "Reconcile ip vrf"
  ansible.builtin.include_role:
    name: _reconcile
  vars:
    rcfg_path: ip vrf
    rcfg_data: "{{ routeros_ip_vrf }}"
    rcfg_purge: "{{ routeros_ip_vrf_purge | default(false) }}"
    rcfg_order: false
