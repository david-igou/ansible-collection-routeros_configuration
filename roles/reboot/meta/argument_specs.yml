---
argument_specs:
  main:
    short_description: Reboot or shut down a RouterOS device.
    description:
      - Reboots and waits for the API, or (gated) shuts the device down.
    examples: |
      # Reboot and wait for the API to return (the default).
      - hosts: routers
        gather_facts: false
        roles:
          - role: david_igou.routeros_configuration.reboot

      # Reboot with a longer reconnect wait for a slow device.
      - hosts: routers
        gather_facts: false
        roles:
          - role: david_igou.routeros_configuration.reboot
            vars:
              routeros_reboot_timeout: 600

      # Shut the device down instead of rebooting (no reconnect wait; gated).
      - hosts: routers
        gather_facts: false
        roles:
          - role: david_igou.routeros_configuration.reboot
            vars:
              routeros_reboot: false
              routeros_shutdown: true
    options:
      routeros_reboot:
        type: bool
        default: true
        description: Reboot the device and wait for the API to return.
      routeros_shutdown:
        type: bool
        default: false
        description: Shut down the device instead (gated; no reconnect wait).
      routeros_reboot_timeout:
        type: int
        default: 300
        description: Seconds to wait for the API after the reboot.
      routeros_api_hostname:
        type: str
        default: "{{ inventory_hostname }}"
        description: API hostname/IP of the device.
      routeros_api_username:
        type: str
        default: admin
        description: API username.
      routeros_api_password:
        type: str
        description: API password. Secret — supply via vault.
      routeros_api_tls:
        type: bool
        default: true
        description: Use TLS (api-ssl).
      routeros_api_validate_certs:
        type: bool
        default: true
        description: Validate the device TLS certificate.
      routeros_api_port:
        type: raw
        default: ""
        description: TCP port; empty lets the module choose from tls.
