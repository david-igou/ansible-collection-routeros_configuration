# Python packages required for testing this collection. CI's molecule jobs
# install exactly this file (plus ansible-core); collection dependencies are
# resolved by the Makefile `install` target the molecule targets depend on.
pytest-ansible
pytest-xdist
molecule
# libssh backend for network_cli — paramiko fails to negotiate RouterOS SSH;
# the chr molecule scenario connects via community.routeros over libssh.
ansible-pylibssh
# Note: the chr scenario also needs the `sshpass` and `qemu`/`genisoimage`
# system packages (see extensions/molecule/chr/README.md).
# community.routeros's API modules (api_modify/api_info) — used by the
# subsystem roles and their molecule verify steps — require librouteros on
# the Ansible controller.
librouteros>=3.0.0
